Flask==3.0.3
Flask-Login==0.6.3
Werkzeug==3.0.3
face_recognition==1.3.0
opencv-python-headless==4.10.0.84
numpy>=2.1.3
pandas>=2.2.3
openpyxl==3.1.5
Pillow==10.4.0

# NOTE on installation:
# face-recognition depends on dlib, which needs CMake + a C++ compiler.
#
# Windows : easiest route is  pip install cmake  then  pip install dlib  then the rest.
#           If dlib fails to build, install the prebuilt wheel that matches your
#           Python version from: https://github.com/z-mahmud22/Dlib_Windows_Python3.x
# macOS   : brew install cmake && pip install -r requirements.txt
# Linux   : sudo apt-get install cmake build-essential && pip install -r requirements.txt
